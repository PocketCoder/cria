real-vikunja-git
node_modules
dist
dist-ssr
*.local
# Local SQLite databases (dev + test)
*.db
*.db-journal
*.db-wal
*.db-shm

.env
.env.local

.DS_Store
.vscode/
.idea/
*.log

# Tauri
src-tauri/target/
src-tauri/gen/

# Generated API types are committed (see SPEC §6.1). Regenerate with
# `pnpm generate:api`; review the diff in code review.

# Compiled JS from tsc (noEmit: false — safety)
*.js

# Test
coverage/
.aider*
PROJECT-VIEWS-REMAINING.md
